# 2026.249.2311 — feat(member-tree): Cutover C settled contracts — spec v0.2026249.2215 + sdk v0.2026249.2239 (the imageless agent_provisioned envelope fix)

## Summary

Migrates plugin-build (the build-drive plugin, 9th compiled-in plugin of the wave) to the Cutover C settled contracts:

- **Re-pin** `github.com/opencharly/spec` `v0.2026243.707` → **v0.2026249.2215** (the member-tree IR — uniform ordered `Member` tree with position-derived `DeployLevelMembers`/`InSubstrateMembers`/`MemberByName`/`HasMembers` consult surface — plus the substrate declared-fields channel) and `github.com/opencharly/sdk` `v0.2026243.714` → **v0.2026249.2239** (the substrate parse fix + `DeployDeclaredFields` threading); `go mod tidy`.

- **The semantic fix** (proven by the charly worker via the distro-arch `check-agent-live` repro): `build:project` serves the resolved-project envelope every downstream consumer reads. Under the pre-wave contract the canonical **imageless agent_provisioned iterate-entity pod** (`agent_provisioned: true` + `iterate:` block, NO `image:`) failed the envelope's substrate parse — the `iterate:` block was misclassified as an in-substrate member (kind-word key `agent:` inside its value mapping), the parse aborted, and the node was dropped from the envelope `Deploy` map ("no entity check-agent-live"). The fix is the settled-contract re-pin itself: on sdk 2239 a declared `#Deploy` field is DATA (its value is never looked inside), and the settled spec-2215 pod schema accepts the imageless shape (`image` optional; `agent_provisioned` declared; the `ValidateDeployRequiresBox` validator exempts `AgentProvisioned`). No plugin-local CUE input-def change exists to make — this plugin's registered schema is the self-contained `#BuildDispatch` def with `InputDef: ""` on all four words (the build requests are HOST-constructed, never user-authored); the pod input-def schema lives in the embedded spec/sdk contract modules, both now settled pins. Disposition recorded here per R1 (documentation-vs-reality checked: schema/build.cue's own comment already documents the empty-InputDef contract accurately).

- **Identifiers migrated**: ZERO compile breaks — the plugin never touched the removed surface. Executed sweeps below.

## How tested

```
$ go build ./... && go vet ./...
(no output — clean)

$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-build/candy/plugin-build	0.235s
?  	github.com/opencharly/plugin-build/candy/plugin-build/cmd/serve	[no test files]

$ go test -count=1 -run 'TestBuildProjectEnvelope' -v ./...   # the repro proof
=== RUN   TestBuildProjectEnvelope_ImagelessAgentProvisionedPodSurvives
--- PASS: TestBuildProjectEnvelope_ImagelessAgentProvisionedPodSurvives (0.01s)
=== RUN   TestBuildProjectEnvelope_ImagefulPodStillParses
--- PASS: TestBuildProjectEnvelope_ImagefulPodStillParses (0.00s)
=== RUN   TestBuildProjectEnvelope_FleetGateAndMemberTree
    --- PASS: TestBuildProjectEnvelope_FleetGateAndMemberTree/agent-provisioned-stays-in-fleet
    --- PASS: TestBuildProjectEnvelope_FleetGateAndMemberTree/plain-imageless-pod-still-rejected
    --- PASS: TestBuildProjectEnvelope_FleetGateAndMemberTree/deploy-level-sibling-member-tree
PASS
ok  	github.com/opencharly/plugin-build/candy/plugin-build	0.017s
```

### B14(d) — the ACTUALLY-EXECUTED sweeps (commands + real output, on head 8a07bdc789)

Removed-surface grep (removed Cutover C identifiers + ResourceKind):
```
$ git grep -nE 'IsGroup|SortedMemberKeys|SortedNestedKeys|ValidateDeploymentChildren|HasChildren|ResourceKind' -- 'candy/**/*.go'; echo "exit=$?"
exit=1
```
(exit 1 = zero matches across every Go file in the tree)

Dual-map consult grep (`.Children`/`.Members`):
```
$ git grep -nE '\.Children\b|\.Members\b' -- 'candy/**/*.go'; echo "exit=$?"
candy/plugin-build/resolve_project_substrate_test.go:153:	if len(pn.Children) != 1 || pn.Children[0].Name != "watcher" {
candy/plugin-build/resolve_project_substrate_test.go:154:		t.Fatalf("children = %+v, want just the deploy-level sibling watcher", pn.Children)
candy/plugin-build/resolve_project_substrate_test.go:195:	if len(pn.Children) != 1 || pn.Children[0].Name != "watcher" {
candy/plugin-build/resolve_project_substrate_test.go:196:		t.Fatalf("children = %+v, want just the deploy-level sibling watcher", pn.Children)
exit=0
```
Disposition (R5 claim-keyed): these four hits are `spec.ParsedNode.Children` — the LIVE parsed-tree edge on `#ParsedNode` (spec cue_types_gen.go:6151, "sub-entity member children ... the recursive Children mirror the deploy tree's member nesting"), asserted in the new repro test. They are NOT the removed `Deploy`/`FleetNode` `.Children`/`.Members` DUAL MAPS: zero non-test Go file touches the removed surface, and the member-tree consult surface (`DeployLevelMembers`/`InSubstrateMembers`/`MemberByName`/`HasMembers`) is exercised in TestBuildProjectEnvelope_FleetGateAndMemberTree over `spec.Deploy`.

Stale-pin grep (the pre-wave contract versions):
```
$ git grep -nE 'v0\.2026243\.(707|714)' -- '*.go' '*.mod' '*.sum' '*.yml' '*.cue'; echo "exit=$?"
exit=1
```
(exit 1 = zero matches — no old-version residue in go.mod/go.sum/code/comments)

### Repro test

`candy/plugin-build/resolve_project_substrate_test.go` MIRRORS sdk's `TestParse_SubstrateDeclaredIterateStaysData` at the plugin level over the same exported primitives the envelope resolve runs (`loaderkit.ParseDoc` + `ValidateNodeDocCUE` + `ValidateNodeFormSteps` + `spec.ValidateDeploymentTree`), threading the live host snapshot incl. the `DeployDeclaredFields` channel:

1. imageless agent_provisioned pod + iterate survives: both CUE gates accept it, the node survives with iterate data intact, the only member is the deploy-level sibling watcher;
2. imageful pod still parses (legacy spelling unchanged);
3. the Fleet gate keeps the imageless agent_provisioned node in the envelope Deploy map; the discriminator (same node WITHOUT the flag) is still rejected with "lacks required `box:`"; the member-tree consult sites classify the fedora migrated deploy-level sibling spelling (`chrome:` next to `pod:`) as alongside, authored order preserved.

## Rulebook compliance

- **R0** — skills loaded per dispatcher before the first tool call (git-workflow, strict-policy); repo has no local AGENTS.md/CLAUDE.md (checked; umbrella rulebook applied).
- **R1** — RCA on the semantic break: the drop gate was located precisely (substrate-branch member scan misclassifying `iterate:`, fixed by sdk #221/#225; the Fleet gate's `ValidateDeployRequiresBox` exempts `AgentProvisioned` under the settled spec — verified live in the discriminator test, not assumed). RCA on the round-1 BLOCK: the body described the `ResourceKind` and stale-pin sweeps without pasting their real output — fixed here by pasting every executed command with its raw output.
- **R2** — one cutover, one PR: pin bump + repro test only; no unrelated drift.
- **R3** — no duplication: the repro test calls the EXPORTED loaderkit/spec gates the load chain runs; no re-implemented validation; the fixture threading mirrors the sdk guard fixture verbatim (documented as such).
- **R4** — no workarounds: zero sleeps/retries/hand-pins; sweeps executed as bounded git grep (no SIGPIPE head-pipes).
- **R5** — nothing legacy to delete (no removed-surface usage existed to migrate — sweep pasted above); stale-pin sweep confirms no old-version residue; the four `.Children` test hits are the live `ParsedNode` field, dispositioned above.
- **R6** — git safety: work driven at `git -C <abs>` from the umbrella; `git status` clean before commit; no force-push, no hook bypass, no direct main push.
- **R7** — gate proven: `go build`/`go vet`/`go test -count=1 ./...` all green, output pasted above (full-suite run on the final tree).
- **R8** — generated artifacts: none touched.
- **R9** — n/a (no runtime deploy in this change; the semantic shape is pinned by unit repro).
- **R10** — fresh proof from the final committed tree: the gate suite ran against the committed head (working tree clean); PR opened only after PASS.

## Change classification

- **Change class**: dependency re-pin (contract modules) + test-only addition (repro unit test). No behavioral plugin code changed — the semantic fix rides the settled contract pins.
- **Verification gate**: `go build`/`go vet`/`go test -count=1 ./...` on the final committed tree (this repo has no `task verify` — it is a single Go-module candy repo; the Go gate IS the repo gate), plus the executed B14(d) sweeps.
- **Attribution tier**: `fully tested and validated` — gates passed on the final tree, changed paths executed live (the new tests run the exact parse/validate chain the envelope serves).
- **Wave coupling**: this release tag UNBLOCKS the charly group-removal PR and the distro bed R10s (9th of the wave's compiled-in plugin pins).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*